#include "holberton.h"
/**
 * _isdigit - print caracter
 * @c: the integer print
 * Return: the variable digit
 */
int _isdigit(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (0);
	}
	else
	{
		return (1);
	}
}

